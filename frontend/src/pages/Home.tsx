import { toast } from "react-toastify";

const Home = () => {
  return (
    <div>
      <div>
        <h1>Home</h1>
        <button onClick={() => {}}>Click Me</button>
        <button onClick={() => {}}>Click</button>
      </div>
    </div>
  );
};

export default Home;
